# Fruits Store

This project showcases a small fruits store built with **React** and **Vite**. It lists fruits, vegetables and milk products with a simple checkout modal.

## Getting Started

Install dependencies and run the development server:

```bash
npm install
npm run dev
```

Open the printed local URL in your browser to explore the site.
